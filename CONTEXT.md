# 项目上下文

## 目标

将用户提供且有权商业使用的照片转化为一张 A5 竖版纸质贴纸页。成品应含 6 枚可单独模切的贴纸，并可直接交给数码模切店。

## 输入契约

- 一张照片，格式可以是 JPG、PNG、HEIC 或 WebP。
- 用户须声明照片是否由其拍摄，以及是否拥有画面中人物、场地与可识别素材的商业使用权；不确定的素材按高风险处理。
- 用户若未指定风格，默认采用原创、扁平、色彩克制的贴纸插画风，而非临摹任何在世艺术家或品牌风格。

## 输出契约

`outputs/` 下每个订单应包含：

- `a5-six-stickers.svg`：148 × 210 mm 的自包含 SVG，含 6 个贴纸和 `CutContour` 刀线图层；
- `source-original.<ext>`：输入原图的字节级副本；
- `delivery-report.md`：规格、位置/尺寸、源文件 SHA-256、合规检查和假设。

文件名可按订单区分，但不得用临时文件或外部资源代替正式交付。

## 当前状态（2026-09-09）

- 已建立项目级生产、合规和验收规则。
- 已实现 `forge.py` 本地装配器：验证来源文件、拒绝未确认的权利声明、逐字节复制原图、内嵌六个已审核资产、创建 A5 SVG 刀线层和交付报告。
- 已实现离线单元测试，验证原图哈希、六枚计数、内嵌资源、刀线层和不安全声明拦截。
- 未绑定任何生图服务。后续 Agent 必须按照 `docs/ARTWORK_CONTRACT.md` 选择用户认可的视觉制作方式；项目本身不得暗中上传照片。
- 尚未创建任何客户订单的正式交付物。

## 生产决策

- 默认选择 6 个低风险、彼此有辨识度的主体，优先随身物、自然元素和通用物件。
- 先确定六个元素及风险处置，再进行插画和几何排版。
- 版面、间距、安全边距和刀线由 SVG 坐标验证；图像生成仅可用作插画来源，不可替代印前规则。
- 如果照片缺少六个合规主体，可通过照片色彩、天气、纹理或通用旅行物补充，不得引入品牌、角色或网络素材。

## 已知限制

- “商用可用”依赖用户对输入照片与画面内容的权利；本项目可以识别并降低常见风险，不能替代律师的权利审查。
- 不同模切厂的刀线命名、出血和文件偏好可能不同。默认规范是 `CutContour` / `#FF00FF` / 0.25 pt / kiss cut；客户有印厂模板时，应以其模板为准并记录差异。
- 当前刀线形状为圆角矩形，以确保所有 6 个资产都具备可验证的闭合刀线。若需要异形轮廓，必须新增经测试的轮廓输入格式与碰撞/留白验证，不可通过手改导出的 SVG 绕过检查。

