# 项目上下文

## 目标

将用户提供且有权商业使用的照片转化为一张 A5 竖版纸质贴纸页。成品应含 6 枚可单独模切的贴纸，并可直接交给数码模切店。

## 输入契约

- 一张照片，格式可以是 JPG、PNG、HEIC 或 WebP。
- 用户须声明照片是否由其拍摄，以及是否拥有画面中人物、场地与可识别素材的商业使用权；不确定的素材按高风险处理。
- 用户若未指定风格，默认采用原创、扁平、色彩克制的贴纸插画风，而非临摹任何在世艺术家或品牌风格。
- 走装配层时，额外需要 6 张已人工复核的透明插画资产（见 `docs/ARTWORK_CONTRACT.md`）与一份 `rights.json`。

## 输出契约

`outputs/` 下每个订单应包含：

- `a5-six-stickers.svg`：148 × 210 mm 的自包含 SVG，含 6 个贴纸和 `CutContour` 刀线图层；
- `source-original.<ext>`：输入原图的字节级副本；
- `delivery-report.md`：规格、位置/尺寸、源文件 SHA-256、合规检查和假设；
- `manifest.json`：机读清单。

文件名可按订单区分，但不得用临时文件或外部资源代替正式交付。

## 当前状态（2026-09-10）

项目已从"只有契约"演进为**三层可运行实现**，详见 `docs/ARCHITECTURE.md`。

### 已完成

- **装配层**：`forge.py` + `src/sticker_forge/`。零第三方依赖、不联网、不生图。支持 `preflight` / `rights-template` / `assemble` / `verify` 四个命令；验证来源文件、拒绝未确认的权利声明、逐字节复制原图、内嵌六个已审核资产、创建 A5 SVG 刀线层和交付报告，并可独立复核任意 A5 SVG。
- **产线层**：`engine/`。完整 Python 产线，含生图 Provider 抽象、三道合规闸（ingest / generation / egress）、边缘连通色度键抠图、alpha 连通域切分、异形刀版矢量化、A5 排版与审计报告，以及红队样本与回归工具。
- **交互层**：`console/`。EdenX + React 19 + TypeScript 操作台，在浏览器本地完成抠图、切分、刀版、A5 排版、质检与下载。
- **规格统一**：三层全部对齐 `AGENTS.md` 0.3 的印刷契约——148 × 210 mm、kiss-cut 默认无出血、安全边距 10 mm、刀线净距 8 mm、单枚 22–62 mm（硬下限 20 mm）、`CutContour #FF00FF` 0.25 pt。
- **测试**：`tests/test_forge.py` 共 24 项离线单测，覆盖图头解析、原图哈希与字节保全、权利声明各类拒绝路径、六枚计数、几何与图层、刀线闭合、低分辨率拒绝、验收器自洽与篡改捕获、engine 与装配层规格一致性。全部通过。
- **示例资源**：`assets/samples/` 下脱敏示例台纸、预览与 6 枚贴纸资产，实测刀线最小净距约 12.19 mm、最小安全边距约 12.02 mm、最低有效 dpi 约 400。
- **文档**：`docs/` 下印刷规格、视觉资产契约、合规策略、生图模型与额度、架构说明。

### 未完成 / 边界

- 装配层**永远不会**绑定生图服务；生图能力只存在于产线层，且默认走平台内置图像 skill（底层默认模型 `image-gen`，可由环境变量覆盖，见 `docs/MODEL_AND_QUOTA.md`）。
- 项目不得暗中上传用户照片。任何外发传输都必须是用户对该次传输的明确授权。
- 尚未创建任何真实客户订单的正式交付物；`outputs/` 中仅有本地端到端验证的临时结果，且已被 `.gitignore` 排除。
- 异形刀线仅在产线层可用；装配层保守使用可验证的圆角矩形。

## 生产决策

- 默认选择 6 个低风险、彼此有辨识度的主体，优先随身物、自然元素和通用物件。
- 先确定六个元素及风险处置，再进行插画和几何排版。
- 版面、间距、安全边距和刀线由 SVG 坐标验证；图像生成仅可用作插画来源，不可替代印前规则。
- 如果照片缺少六个合规主体，可通过照片色彩、天气、纹理或通用旅行物补充，不得引入品牌、角色或网络素材。
- 一次生图产出六宫格大图，而非六次单图：省额度且风格天然统一。

## 已知限制

- “商用可用”依赖用户对输入照片与画面内容的权利；本项目可以识别并降低常见风险，不能替代律师的权利审查。
- 不同模切厂的刀线命名、出血和文件偏好可能不同。默认规范是 `CutContour` / `#FF00FF` / 0.25 pt / kiss cut；客户有印厂模板时，应以其模板为准并记录差异。
- 当前装配层刀线形状为圆角矩形，以确保所有 6 个资产都具备可验证的闭合刀线。若需要异形轮廓，必须新增经测试的轮廓输入格式与碰撞/留白验证，不可通过手改导出的 SVG 绕过检查。
- 自动验收只覆盖几何与文件结构；内容合规、有效分辨率是否满足具体印厂、跨机器打开测试仍需人工。
