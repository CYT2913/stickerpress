# AGENTS.md

本文件供 AI Agent 接手本项目时阅读。第 0 节反映当前实现状态；第 2 节为固定协作纪律。

---

## 0. 项目信息

### 0.1 这个项目是做什么的

将一张用户有权商用的现场照片，制作成可交付印厂的 **A5 纸质贴纸版**。每张版面含 6 枚独立贴纸，最终交付：

- 一份 A5 SVG：含完整插画、6 个可辨识贴纸和 `CutContour` 刀线图层；
- 一份用户提供的原始照片副本，文件内容不得被改写；
- 一份交付报告：记录规格、源文件哈希、假设和合规检查结果。

当前仓库提供一个**本地、无网络依赖的 SVG 装配器**；它不生成或分析图像。视觉 Agent 必须依照 `docs/ARTWORK_CONTRACT.md` 生成并人工复核 6 张透明插画资产，再交给装配器完成印前工作。不要把测试中的 1 px 示例图或通用占位资产当成交付。

### 0.1.1 运行与验证

| 项 | 值 |
|---|---|
| 语言 | Python 3.9+ |
| 第三方运行依赖 | 无 |
| 入口 | `python forge.py` |
| 单元测试 | `python -m unittest discover -s tests -v` |

常用命令：

```powershell
# 只验证来源文件和记录 SHA-256；不会生成输出，也不会调用网络
python forge.py preflight C:\path\to\photo.jpg

# 由 Agent 提供六张已审核 PNG/JPEG/WebP 和 rights.json 后，输出正式交付
python forge.py assemble C:\path\to\photo.jpg `
  --artwork-dir C:\path\to\six-artworks `
  --rights C:\path\to\rights.json `
  --outdir outputs\order-001 `
  --order-id order-001
```

改动 `src/sticker_forge/`、`forge.py`、交付规格或权利校验逻辑后，必须运行全部单元测试。修改视觉生成策略前必须阅读 `docs/ARTWORK_CONTRACT.md`。

### 0.2 交付与文件安全

- 客户照片、成品位图、SVG 导出物和订单文件属于私密材料：不得提交到 Git 仓库。
- 处理中间文件放在 `work/`；仅将面向用户的正式交付放在 `outputs/`。
- 原图副本保留原始扩展名及字节内容；交付报告须记录 SHA-256。
- `CHANGELOG.md` 只增不改：新条目写在最上方，不重写历史。
- 每次产出都应记录在 `ASSUMPTIONS.md` 或交付报告中：假设、依据、选错影响和返工成本。

### 0.3 印刷规格与验收门槛

| 项目 | 规范 |
|---|---|
| 成品画布 | A5 竖版，148 × 210 mm |
| 贴纸数量 | 正好 6 枚 |
| 安全边距 | 四边至少 10 mm |
| 贴纸最小净距 | 至少 8 mm（含刀线） |
| 单枚尺寸 | 建议 22–62 mm；不得小于 20 mm 的任一方向 |
| 刀线 | 独立 `CutContour` 图层，`#FF00FF`、0.25 pt、闭合路径、半切 kiss-cut |
| SVG | 必须自包含：不得依赖本地或远程外部位图；若使用位图，须 data URI 内嵌 |
| 印刷参考 | 400 dpi 对应约 2331 × 3307 px；若提供 PNG 预览，需明确其实际像素与 DPI |

不得把排版、刀线闭合、间距或安全边距交给生成模型“自行遵守”；这些必须在 SVG 几何中可核查。

### 0.4 商用合规边界

生成贴纸不自动取得商业使用权。处理照片前，优先核对用户是否拥有照片著作权，以及其中可识别人物、私有场地或素材的商用授权。

- **禁止直接复现**：商标、品牌名、Logo、产品包装、版权角色、盲盒/手办形象、景区吉祥物、文创商品设计、票券二维码、车牌、联系方式与其他隐私标识。
- **保守排除**：现代受保护建筑的可识别主体、受版权保护的艺术装置、海报和摄影作品；授权不明时，改为自然元素、普通物件或抽象背景。
- **优先选材**：用户随身物、自然景物、食物、植物、通用旅行物与无文字的纪念性物件。
- **人物**：无明确授权时，不保留可识别肖像；可改为背影、剪影或不具识别性的通用人物。
- **三层检查**：输入照片预检、选材/提示词过滤、成品人工复检。任一阶段发现高风险元素，替换或剔除，不以免责声明代替处理。

交付报告必须说明“已检查到的风险与处理方式”，不可承诺绝对法律结论；需要法定授权的内容必须标记为“需用户确认”。

### 0.5 成品质量检查

交付前逐项检查：

1. SVG 根元素为 148 mm × 210 mm，`viewBox` 与实际布局一致。
2. 恰好有 6 个独立贴纸组，每组均有可视内容和一条闭合刀线。
3. 刀线位于 `CutContour` 图层，颜色和线宽符合 0.3；不切穿邻近元素或安全边距。
4. 贴纸之间、贴纸与页面边缘的最小距离符合 0.3。
5. SVG 脱离工作目录也可打开，不出现缺图、缺字体或远程资源依赖。
6. 成品不含未授权的品牌、角色、可识别人物、隐私信息或高风险建筑/艺术品。
7. 原图副本的 SHA-256 与输入文件一致；所有正式文件均在 `outputs/`。

### 0.6 不得做的事

- 不把客户照片、未脱敏素材或成品提交到版本库、公开链接或第三方服务，除非用户明确授权该特定传输。
- 不下载、临摹或拼贴网络图片来补全贴纸素材；没有可用来源时使用原创、抽象或通用图形。
- 不伪造授权、模型能力、分辨率、印厂适配或合规结论。
- 不因“恰好六枚”的数量要求而用高风险元素凑数。

---

## 1. 上手顺序

1. 阅读 `CONTEXT.md`，确认当前状态与输入/输出契约。
2. 阅读 `CHANGELOG.md` 最近两条，了解近期决策。
3. 阅读本文件的 0.3、0.4、0.5，确认印刷和合规红线。
4. 检查 `outputs/` 中是否已有同一订单的正式交付，避免覆盖。
5. 收到照片后，先完成预检和授权状态记录，再开始设计与导出。

---

## 2. 提问纪律

提问消耗用户注意力，默认先动手。

- 只有当不同选择会产生明显不同成品、无法自行验证且选错返工成本很高时，才提问。
- 需要提问时一次问完，并带上项目事实、可行选项、代价、推荐方案及对验收的影响。
- 其他取舍自行采用保守、可逆的方案，并在 `ASSUMPTIONS.md` 或交付报告中说明。
- 照片商业权属、人物肖像授权或用户明确要求保留的高风险元素，属于不可自行假设的事项；缺失时先做不含该元素的保守版本，或请求必要确认。

