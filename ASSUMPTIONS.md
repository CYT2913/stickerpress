# 假设记录

按 `AGENTS.md` 0.2 与第 2 节：自行采用的取舍要写清**假设、依据、选错影响、返工成本**。本文只增不删，过期条目标注"已失效"而不是抹掉。

---

## 2026-09-10 · kiss-cut 台纸默认不留出血

- **假设**：交付默认为 kiss-cut 半切工艺，SVG 根元素即 A5 成品 148 × 210 mm，`bleed_mm = 0`。
- **依据**：kiss-cut 只切面材不切底纸，台纸边缘不参与裁切，不存在"切偏露白"。留出血会让文件变成 154 × 216 mm，与"A5"标称对不上，反而容易被印厂误按整切处理。
- **选错影响**：若客户实际要整切到边（die-cut through），成品四周可能露白。
- **返工成本**：低。`engine/stickerpress/config.py` 的 `bleed_mm` 改为 3.0 即可，画布与裁切标记自动跟随，无需改版面逻辑。

## 2026-09-10 · 刀线线宽按 0.25 pt 而非 0.25 mm

- **假设**：`CutContour` 线宽 = 0.25 pt = 0.088194 mm，代码取 `0.0882 mm`。
- **依据**：`AGENTS.md` 0.3 写的是 0.25 pt。此前实现误写成 0.25 mm，实际约 0.7087 pt，粗 2.8 倍，RIP 可能把它当印刷线条而非刀线，或按粗线中心偏移下刀。
- **选错影响**：刀线被识别成图形、模切位置偏移。
- **返工成本**：低，改常量重出文件；但若已投印则整批报废，因此这条属于必须验证项，已进单测。

## 2026-09-10 · 装配层只出圆角矩形刀线

- **假设**：`forge.py assemble` 输出圆角矩形闭合刀线（`corner_r_mm = 4.0`），不做异形轮廓。
- **依据**：圆角矩形必然闭合、不自交、包围盒可精确计算，因此净距和安全边距能被 `verify` 独立复核。异形轮廓的碰撞与留白验证复杂度高，放在产线层。
- **选错影响**：贴纸外形不贴合主体轮廓，视觉上不如异形好看。
- **返工成本**：中。需新增轮廓输入格式（如每枚一条 path）+ 碰撞/留白验证 + 对应单测；不得靠手改导出 SVG 绕过。

## 2026-09-10 · 装配层零第三方依赖

- **假设**：`forge.py` 与 `src/sticker_forge/` 只用 Python 标准库，包括自写 PNG/JPEG/WebP 头解析与 SVG path 解析。
- **依据**：交付层必须在任意装了 Python 3.9 的机器上断网可跑，且不能因为一次库升级改变输出。同时 `verify` 不复用 `assemble` 的几何逻辑，两边独立实现才有交叉校验价值。
- **选错影响**：自写解析只覆盖交付所需的格式与命令子集，遇到罕见变体会报错而不是静默处理。
- **返工成本**：低。报错即暴露，按需补分支并加单测。

## 2026-09-10 · 一次生成六宫格大图而非六次单图

- **假设**：产线层每单只调用 1 次生图，产出 2:3 六宫格大图，再本地切分。
- **依据**：省约 6 倍生图额度；六枚在同一次生成中风格天然统一，不需要额外的风格对齐环节。
- **选错影响**：其中一枚不合格时需要整张重生成，重试成本是单图方案的 6 倍。
- **返工成本**：低。`SheetFileProvider` 支持复用现成大图重跑排版，调版面不消耗额度。

## 2026-09-10 · 品红色度键而非要求模型输出透明背景

> ⚠️ **已部分失效**，2026-09-12 修正为"优先真 alpha、品红降为兜底"，见本文末条目。

- **假设**：让模型在纯品红 `#FF00FF` 背景上作画，本地做边缘连通色度键转真 alpha。
- **依据**：直接要求"透明背景"，模型多数会**画出**棋盘格纹理，没有真 alpha。只移除与画面边缘连通的品红，可避免主体内部同色区域被误穿孔。
- **选错影响**：主体若大面积使用品红或接近品红的洋红，会被误抠。
- **返工成本**：低。选材阶段规避品红主体，或换用其他罕见键色重跑。

## 2026-09-10 · 报告中的模型字段是标签而非底层模型 ID

- **假设**：`GenerationResult.model = "platform-image-edit"` 表示"走平台图像编辑入口"，底层实际默认模型为 `image-gen`。
- **依据**：底层模型由平台 skill 内部决定，且可被 `AIME_MULTIMODAL_NAME` 覆盖，本仓库无法在调用侧稳定断言。
- **选错影响**：读报告的人可能误以为 `platform-image-edit` 是模型名。
- **返工成本**：低。若需在报告中写实际模型，应在调用侧显式指定并回写，不得凭猜测填写（`AGENTS.md` 0.6 禁止伪造模型能力）。

## 2026-09-10 · 仓库内保留脱敏示例资源

- **假设**：`assets/samples/` 与 `console/config/public/demo/` 下的示例台纸、预览与 6 枚贴纸资产为**自制脱敏素材**，允许进版本库，`.gitignore` 为其开例外。
- **依据**：没有示例，接手者无法验证装配与前端渲染是否正常；这些素材不含任何客户照片、可识别人物或第三方 IP。
- **选错影响**：若误把客户素材放进这两个目录，会随仓库外泄。
- **返工成本**：高（一旦推送到公开仓库即不可撤回）。因此规则是：**这两个目录只放自制素材，客户文件一律走 `work/` 与 `outputs/`**，二者已被全局忽略。

## 2026-09-10 · 合规 fail-mode 取 review

- **假设**：VLM 审核不可用、超时或解析失败时降级转人工，而非放行。
- **依据**：实体贴纸印出后无法召回，漏检成本远高于误拦成本。
- **选错影响**：后端抖动时人工队列积压，交付变慢。
- **返工成本**：低。`policy.yaml` 的 `fail_mode` 改为 `pass` 即可，但仅建议用于压测环境。

## 2026-09-12 · 背景透明化优先走真 alpha，品红色度键降为兜底

- **假设**：`prepare_rgba()` 先检测输入是否自带可用 alpha。有则**直通**（`mode="alpha"`），完全不做色度键；没有才退回品红色度键（`mode="chroma"`）。
- **依据**：实测 ChatGPT 贴纸功能导出的 PNG **确实带真 alpha**，查看器里的灰白棋格只是底衬而非文件内容。此前"模型给不出真 alpha"的判断过于保守，且旧 `pipeline` 无条件调用 `key_out()`，其内部 `convert("RGB")` 会把真 alpha 丢掉——背景变不透明，六枚粘成一个连通域，只能切出 1 枚。真 alpha 直通没有溢色抑制与阈值损失，连通域切分也更准。
- **选错影响**：若某些来源的 alpha 是"全不透明的空 alpha 通道"（有通道但无透明像素），直通会误判。已用 `has_usable_alpha()` 要求存在实际透明像素来规避；再加 `looks_like_painted_checkerboard()` 兜住"画出来的棋盘格"这类假透明，该情况无法补救，只能重出图。
- **返工成本**：低。判别逻辑集中在 `chroma.py` 一个入口，阈值可调；`engine/tools/check_alpha.py` 可对任意图片单独复核结论与切分数量，已进单测（`tests/test_imaging_alpha.py`，含"直接 key_out 会毁掉真透明图"的固化回归）。
